{
  "openapi": "3.0.0",
  "info": {
    "version": "0.1.0",
    "title": "Swagger TreatOptions",
    "description": "Treatment Options REST API",
    "contact": {
      "url": "http://standardhealthrecord.org",
      "email": "gquinn@mitre.org"
    }
  },
  "servers": [
    {
      "url": "http://localhost:3001/api"
    }
  ],
  "paths": {
    "/criteriaForCondition": {
      "get": {
        "summary": "Get available criteria for condition",
        "description": "Return criteria for condition",
        "operationId": "getCriteriaForCondition",
        "parameters": [
          {
            "name": "Condition",
            "in": "query",
            "required": true,
            "description": "The condition that we are getting criterias for",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
           "200": {
            "description": "array of available criteria for condition",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                }
              }
            },
            "headers": {
              "Access-Control-Allow-Origin": {
                "schema": {
                  "type": "string",
                  "default": "*"
                },
                "description": "Setting this header to `*` allows all origins. This is handy for public REST APIs that don't require authentication. But, according to the HTTP spec, browsers *WILL NOT* send cookies if this header is `*`, regardless of what you set `Access-Control-Allow-Credentials` to.\n"
              }
            }
          },
          "404": {
            "description": "Condition not found",
            "headers": {
              "Access-Control-Allow-Origin": {
                "schema": {
                  "type": "string",
                  "default": "*"
                },
                "description": "Setting this header to `*` allows all origins. This is handy for public REST APIs that don't require authentication. But, according to the HTTP spec, browsers *WILL NOT* send cookies if this header is `*`, regardless of what you set `Access-Control-Allow-Credentials` to.\n"
              }
            }
          }
        }
      }
    },
    "/treatmentOptions": {
      "get": {
        "summary": "Get treatment options given filters",
        "description": "Return treatment options for the following filters",
        "operationId": "findTreatmentOptionsByCriteria",
        "parameters": [
          {
            "name": "Condition",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "Criteria",
            "in": "query",
            "required": false,
            "description": "Criteria as name/value pair on object",
            "schema": {
              "$ref": "#/components/schemas/Criteria"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "array of treatment options for given filters",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/TreatmentOptions"
                  }
                }
              }
            },
            "headers": {
              "Access-Control-Allow-Origin": {
                "schema": {
                  "type": "string",
                  "default": "*"
                },
                "description": "Setting this header to `*` allows all origins. This is handy for public REST APIs that don't require authentication. But, according to the HTTP spec, browsers *WILL NOT* send cookies if this header is `*`, regardless of what you set `Access-Control-Allow-Credentials` to.\n"
              }
            }
          },
          "400": {
            "description": "Invalid filter: condition criteria required",
            "headers": {
              "Access-Control-Allow-Origin": {
                "schema": {
                  "type": "string",
                  "default": "*"
                },
                "description": "Setting this header to `*` allows all origins. This is handy for public REST APIs that don't require authentication. But, according to the HTTP spec, browsers *WILL NOT* send cookies if this header is `*`, regardless of what you set `Access-Control-Allow-Credentials` to.\n"
              }
            }
          },
         "404": {
            "description": "No treatment options found for specified criteria",
            "headers": {
              "Access-Control-Allow-Origin": {
                "schema": {
                  "type": "string",
                  "default": "*"
                },
                "description": "Setting this header to `*` allows all origins. This is handy for public REST APIs that don't require authentication. But, according to the HTTP spec, browsers *WILL NOT* send cookies if this header is `*`, regardless of what you set `Access-Control-Allow-Credentials` to.\n"
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "Criteria": {
        "type": "object",
        "properties": {
          "AgeAtDiagnosis": {
            "type": "integer"
          },
          "Gender": {
            "type": "string"
          },
          "Race": {
            "type": "string"
          }
        },
        "additionalProperties": {
          "type": "string"
        },
        "example": {
          "AgeAtDiagnosis": 54,
          "Gender": "male",
          "Race": "white",
          "KIT": "positive",
          "PDGFRA": "negative",
          "Grade": "3",
          "Stage": "IIIA"
        }
      },
      "TreatmentOptions": {
        "type": "object",
        "properties": {
          "condition": {
            "type": "string",
            "description": "condition"
          },
          "race": {
            "type": "string",
            "description": "race filter"
          },
          "isAlive": {
            "type": "string",
            "description": "dead or alive"
          },
          "survivalMonths": {
            "type": "integer",
            "format": "int32",
            "description": "survival time in months"
          },
          "grade": {
            "type": "string",
            "description": "grade of the disease to filter"
          },
          "treatOptions": {
            "type": "string",
            "description": "treatment option recieved"
          }
        }
      }
    }
  }
}